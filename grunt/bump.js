module.exports = {
    options: {
        files: ['package.json', 'bower.json'],
        updateConfigs: [],
        commit: true,
        createTag: false,
        push: true,
        globalReplace: true
    }
}